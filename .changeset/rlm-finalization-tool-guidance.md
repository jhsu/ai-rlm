---
"ai-rlm": minor
---

Add optional `description` guidance to `createRLMTool`, respect `FINAL()` and `FINAL_VAR()` results returned from executed JavaScript code blocks, enforce QuickJS execution timeouts, return arrays from `llm_query_batched()`, and improve small-context previews and final-answer prompting.
