# Implementation Plans

Generated by the improve skill on 2026-06-10. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Restore Typecheck As A Reliable Gate | P1 | S | — | DONE |
| 002 | Standardize Package Manager And Lockfiles | P1 | M | 001 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 002 depends on 001 because package-manager standardization should preserve a known-good `typecheck` command instead of changing install tooling while the TypeScript gate is already broken.

## Reconciliation log

- 2026-06-13, commit `9ec1fde` (`advisor/002-standardize-package-manager`): verified DONE plans 001 and 002. `rtk npm run typecheck`, `rtk npm run typecheck:monitor`, and `rtk tsc --noEmit` all exited 0. Root package-manager state has `packageManager: npm@11.13.0`, one root lockfile (`package-lock.json`), and release install `npm ci --include=dev --ignore-scripts`.
- 2026-06-13 audit check: `rtk npm audit --include=dev --json` completed with 5 known vulnerabilities still present: `brace-expansion`, `esbuild` via `wrangler`, and `fast-xml-parser` via `just-bash`. This remains a follow-up dependency-remediation plan, not part of Plan 002.

## Unplanned follow-ups

- Dependency vulnerability remediation is now unblocked by Plan 002. Use `package-lock.json` and npm as the source of truth for any follow-up plan.

## Findings considered and rejected

- Cloudflare sandbox timeout enforcement: not selected for planning in this pass; executor contract may already enforce timeout, and Cloudflare replay semantics are the higher-confidence sandbox issue.
- Broad `RLMAgent` refactor: not selected for planning in this pass; should wait until characterization tests exist.
- Secret hygiene for `.env`: high-confidence security finding, but not selected by the user for this plan-writing pass.
